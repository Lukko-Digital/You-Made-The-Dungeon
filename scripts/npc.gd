extends Area2D

const DIALOGUE = {
	'first interaction': ['zao shang hao zong guo', 'xian zai wo you bing qi lin', 'wo hen xi huan bing qi lin'],
	'later interaction': ['yuh'],
	'end interaction': ['...'],
}

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
